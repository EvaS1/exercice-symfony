{% extends 'base.html.twig' %}

{% block body %}

<ul>
	{% for ami in amiedit %}
		<li>{{ami.id}} {{ami.nomAmi}} {{ami.prenomAmi}} {{ville.nomVille}}</li>
	{%endfor%}
</ul>

<h1>Formulaire modification ami</h1>

{{ form_start(form) }}    {# Ouvre la balise form #}

{{ form_widget(form) }}   {# Génère le contenu du formulaire #}

{{ form_end(form) }}   {# Ferme la balise form #}
  
{% endblock %}
